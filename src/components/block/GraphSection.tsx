import React from 'react';
import { Graph } from './Graph';

export const GraphSection = () => {
	return (
		<section
			style={{
				width: '100%',
				height: '1000px',
			}}
		>
			<Graph />
		</section>
	);
};
